# SMTP configuration (works for Gmail, Brevo, Mailgun, etc.)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# Brevo Email API (optional if using SMTP instead)
BREVO_API_KEY=

# Google API Key (if the messaging agent reads spreadsheet info)
GOOGLE_API_KEY=
